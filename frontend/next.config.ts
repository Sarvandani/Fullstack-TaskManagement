import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Disable Next.js development indicator (bottom-left 'n' button)
  devIndicators: {
    buildActivity: false,
  },
};

export default nextConfig;
